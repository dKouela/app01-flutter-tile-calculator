{
  "name": "supabase-proxy-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy for Supabase with caching and rate limiting",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env production",
    "deploy-dev": "wrangler deploy --env development"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}